# Production API URL — used by the mobile production APK build
# The app reads this via process.env.EXPO_PUBLIC_API_URL in src/config.js
EXPO_PUBLIC_API_URL=https://farmerp-backend-production.up.railway.app/api/v1
